# MCP Smoke Workspace

This directory is intentionally small and read-only for the aichat MCP smoke test.

Expected behavior:

- `list_directory` should show this file.
- `read_file` should return this file's contents.
- Paths outside this directory should be rejected by the server.
